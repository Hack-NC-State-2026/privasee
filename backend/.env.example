# Application
APP_NAME=Backend API
DEBUG=false
ENVIRONMENT=development

# Gemini
GEMINI_API_KEY=


# Valkey (Redis-compatible key-value store)
# Defaults below assume the local Docker container is published on 127.0.0.1:6379.
VALKEY_HOST=127.0.0.1
VALKEY_PORT=6379
VALKEY_PASSWORD=
